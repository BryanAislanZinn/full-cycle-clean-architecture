# full-cycle-clean-architecture

## Passo a passo

- clonar o projeto via `git clone https://github.com/BryanAislanZinn/full-cycle-clean-architecture.git`
- executar o projeto via `npm install`
- para executar os testes, rodar o comando `npm test`
